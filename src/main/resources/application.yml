server:
  context-path: /
  port: 8013

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test3
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
  http:
    multipart:
      enabled: true
      max-file-size: 500MB
      max-request-size: 500MB
